@page "/tools/{id}"

<h1>Product @Id</h1>

@if(ToolsData == null)  
{
    <p>Loading...</p>
} 
else {
    @foreach (var tool in ToolsData.Tools)
    {
        @if(tool.MarkdownFile == Id) {
            <DPW.Components.MarkdownContent.MarkdownContent MarkdownFileName=@tool.MarkdownPath() />
        }
    }

}

