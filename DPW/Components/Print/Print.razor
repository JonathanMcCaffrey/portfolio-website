@inherits ComponentBase


<style>
    html {
        background-color: transparent;
    }
    @("@page { size:letter } ")
    @("@page{margin:0}body{margin:0}.sheet{margin:0;overflow:hidden;position:relative;box-sizing:border-box;page-break-after:always}div.letter .sheet{width:216mm;height:279mm}@media screen{body{background:#e0e0e0}.sheet{background:#fff;box-shadow:0 .5mm 2mm rgba(0,0,0,.3);margin:5mm auto}}@media print{div.letter{width:216mm}}")
    @("app{position:initial; display:initial;flex-direction:initial}")
    @("@media print { body { width: 210mm;min-width: auto !important;}}")
    article { width:100%;height:100%}
</style>

<div class="letter">
    @ChildContent
</div>

@code
{
    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
